/*
 * This file was previously the Cordova/PhoneGap bootstrap.
 * With the migration to Capacitor, the device-ready event is no longer needed.
 * Capacitor automatically injects its bridge on native platforms (iOS/Android).
 * Configuration (StatusBar, SplashScreen, etc.) is handled declaratively
 * via capacitor.config.json.
 */
